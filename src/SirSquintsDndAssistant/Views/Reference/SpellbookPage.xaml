<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SirSquintsDndAssistant.ViewModels.Reference"
             xmlns:models="clr-namespace:SirSquintsDndAssistant.Models.Content"
             x:Class="SirSquintsDndAssistant.Views.Reference.SpellbookPage"
             x:DataType="vm:SpellbookViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto, Auto, *" Padding="10">
        <!-- Search Bar -->
        <SearchBar Grid.Row="0"
                   Placeholder="Search spells..."
                   Text="{Binding SearchText}"
                   Margin="0,0,0,10"/>

        <!-- Level Filter -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12" Margin="0,0,0,10">
            <Picker Title="Spell Level"
                    ItemsSource="{Binding SpellLevels}"
                    SelectedIndex="{Binding SelectedLevel}"/>
        </Border>

        <!-- Spell List -->
        <CollectionView Grid.Row="2" ItemsSource="{Binding Spells}" SelectionMode="None">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="20">
                    <Label Text="No spells found"
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="{StaticResource Primary}"/>
                    <Label Text="Try adjusting your search or run data sync"
                           FontSize="14"
                           TextColor="{StaticResource Gray500}"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Spell">
                    <Border Style="{StaticResource Card}" Margin="0,4">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpellbookViewModel}}, Path=ViewSpellCommand}"
                                CommandParameter="{Binding .}"/>
                        </Border.GestureRecognizers>

                        <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto, Auto">
                            <!-- Spell Name -->
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding Name}"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Parchment}}"/>

                            <!-- Level Badge -->
                            <Border Grid.Row="0" Grid.Column="1"
                                    BackgroundColor="{StaticResource Tertiary}"
                                    Padding="8,4"
                                    VerticalOptions="Start">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="4"/>
                                </Border.StrokeShape>
                                <Label TextColor="{StaticResource White}" FontSize="12">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Lvl "/>
                                            <Span Text="{Binding Level}" FontAttributes="Bold"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </Border>

                            <!-- School and Components -->
                            <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray500}"
                                   Margin="0,4,0,0">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding School}" FontAttributes="Italic"/>
                                        <Span Text=" • "/>
                                        <Span Text="{Binding CastingTime}"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <!-- Description Preview -->
                            <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="{Binding Description}"
                                   FontSize="12"
                                   TextColor="{StaticResource TextSecondary}"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="2"
                                   Margin="0,8,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="2"
                          IsRunning="{Binding IsBusy}"
                          IsVisible="{Binding IsBusy}"
                          Color="{StaticResource Primary}"
                          HorizontalOptions="Center"
                          VerticalOptions="Center"/>
    </Grid>
</ContentPage>
