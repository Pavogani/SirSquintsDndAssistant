<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SirSquintsDndAssistant.ViewModels.Campaign"
             xmlns:models="clr-namespace:SirSquintsDndAssistant.Models.Campaign"
             x:Class="SirSquintsDndAssistant.Views.Campaign.SessionNotesPage"
             x:DataType="vm:SessionNotesViewModel"
             Title="{Binding Title}">
    <Grid ColumnDefinitions="*, 2*" Padding="10" ColumnSpacing="10">
        <CollectionView Grid.Column="0" ItemsSource="{Binding Sessions}"
                        SelectedItem="{Binding SelectedSession}"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding SelectSessionCommand}"
                        SelectionChangedCommandParameter="{Binding SelectedSession}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Session">
                    <Border Padding="10" Margin="0,3" BackgroundColor="#F5F5F5" Stroke="#E0E0E0" StrokeThickness="1">
                        <Border.StrokeShape><RoundRectangle CornerRadius="5"/></Border.StrokeShape>
                        <VerticalStackLayout>
                            <Label Text="{Binding Title}" FontSize="14" FontAttributes="Bold"/>
                            <Label Text="{Binding SessionDate, StringFormat='Session {0:MMM dd}'}" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Grid Grid.Column="1" RowDefinitions="Auto, Auto, *, Auto">
            <Entry Grid.Row="0" Placeholder="Session title" Text="{Binding SessionTitle}" Margin="0,0,0,10"/>
            <Editor Grid.Row="2" Placeholder="Session notes (markdown supported)..." Text="{Binding SessionNotes}"/>
            <Button Grid.Row="3" Text="Save Session" Command="{Binding CreateSessionCommand}"
                    BackgroundColor="#4169E1" TextColor="White" Margin="0,10,0,0"/>
        </Grid>
    </Grid>
</ContentPage>
